Zadání projektu je v přiloženém souboru "Zadání.pdf".

Všechny testy lze spustit naráz následujícím příkazem v terminálu:

```
$ pytest test_Python.py -v -s
```

Případně jednotlivé testy lze spustit zvlášť následovně:

```
$ pytest test_Python.py::'nazev_testu' -v -s
```
